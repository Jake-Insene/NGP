FORMAT RAW AS 'BIN'
ORG 0x00000000
B main

INCLUDE "IO.h"

main:
	MOV R0, IO_PAD_MAIN_BUTTONS & 0xFFFF
	MOVT R0, IO_PAD_MAIN_BUTTONS >> 16

.loop:
	LD R1, [R0]
	AND R1, R1, 0x1
	CMP R1, 0
	BEQ .loop
	
	HALT

.zero 0x400000-$